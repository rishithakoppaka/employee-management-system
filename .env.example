# Database Configuration
# For Docker PostgreSQL (default setup)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=employee_db
DB_USER=postgres
DB_PASSWORD=postgres123

# For local PostgreSQL installation, update these values:
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=employee_db
# DB_USER=postgres
# DB_PASSWORD=your_postgres_password_here

# API Configuration
API_HOST=0.0.0.0
API_PORT=8001
API_BASE_URL=http://localhost:8001

# Notes:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update DB_PASSWORD with your actual PostgreSQL password
# 3. Never commit .env file to version control (it's in .gitignore)
